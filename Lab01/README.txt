App.js is the file that stores the actual code for the app

Package.json Tells the program where to find stuff in all of the folders

Node_modules stores directories of important modules for the app

.gitignore stores what filetype endeings (e.g. .expo, .key, ect) files in the project will use 